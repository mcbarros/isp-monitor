<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Status change report</h1>
    <ul>
      <% @changes.each do |route| %>
        <li><%= route.name %>: <%= route.active ? 'active' : 'inactive' %></li>
      <% end %>
    </ul>
  </body>
</html>
